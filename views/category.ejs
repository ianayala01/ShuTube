<!DOCTYPE html>
<html>
<head>
    <title><%= title %></title>
</head>
<body>
    <h1><%= title %></h1>
    <ul>
        <% media.forEach(item => { %>
            <li><a href="/media/<%= item.name %>"><%= item.name %></a></li>
        <% }) %>
    </ul>
    <br>
    <a href="/">Back to Home</a>
</body>
</html>
