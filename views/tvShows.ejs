<!DOCTYPE html>
<html>
<head>
    <title><%= title %></title>
    <link rel="stylesheet" href="/style.css"/>
</head>
<body>
    <%- include('partials/navbar') %>

     <h1 class="vid-head"><%= title %></h1> <!--browse tv shows page -->
    <ul class="bullet">
        <% shows.forEach(show => { %>
            <li><a class="vid-link" href="<%= show.path %>"><%= show.name %></a></li>
        <% }) %>
    </ul>
    <br>

</body>
</html>