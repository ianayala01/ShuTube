<!DOCTYPE html>
<html>
<head>
    <title><%= title %></title>
    <link rel="stylesheet" href="/style.css"/>
</head>
<body>
    <%- include('partials/navbar') %>
<!-- show/seasons page -->
    <h1 class="vid-head"><%= title %></h1>
    <ul class="bullet">
        <% items.forEach(item => { %>
            <li><a class="vid-link" href="<%= item.path %>"><%= item.name %></a></li>
        <% }) %>
    </ul>
    <br>

</body>
</html>