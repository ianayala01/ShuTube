<nav style="display: flex; gap: 1rem; padding: 1rem; background: #f0f0f0;">
    <a href="/">Home</a>

    <% if (parentPath && parentPath !== '/') { %>
        <a href="<%= parentPath %>">Up One Directory</a>
    <% } %>

    <% if (prevVideo) { %>
        <a href="/media/<%= prevVideo.name %>">Previous</a>
    <% } %>

    <% if (nextVideo) { %>
        <a href="/media/<%= nextVideo.name %>">Next</a>
    <% } %>
</nav>
